name: Deploy to VPS (Disabled)

# This workflow is disabled by default
# To enable VPS deployment:
# 1. Set up VPS_HOST, VPS_USERNAME, and VPS_SSH_KEY secrets in GitHub Actions settings
# 2. Uncomment the 'on' section below
# 3. Remove the placeholder job

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: VPS Deployment Status
        run: echo "ℹ️ VPS deployment workflow is disabled. Configure secrets to enable."
